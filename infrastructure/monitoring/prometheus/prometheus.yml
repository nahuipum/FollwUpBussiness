global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yaml

scrape_configs:
  - job_name: fieldsales-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [backend:9091]
