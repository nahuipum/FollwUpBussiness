package com.nahui.followupbussiness.audit.domain;

/** Controlled vocabulary for the critical action categories supported by the audit module. */
public enum AuditAction {
    AUTHENTICATION,
    RESOURCE_ACCESS,
    CRITICAL_MUTATION
}
