import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
import { afterEach, expect, test, vi } from "vitest";
import { CompanySettingsPage } from "./CompanySettingsPage";
const state = vi.hoisted(() => ({ role: "COMPANY_ADMIN", generation: 1, save: vi.fn(), retry: vi.fn(), reload: vi.fn(), snapshot: { etag: "\"7\"", settings: { timezone: "America/Lima", currency: "PEN", geofenceRadiusMeters: 100 as const, trackingIntervalSeconds: 60 as const, locationRetentionDays: 90 as const, saleEditWindowMinutes: 30 } } }));
vi.mock("../auth/auth", () => ({ getSessionIdentity: () => ({ roles: [state.role] }) }));
vi.mock("./hooks/useCompanySettings", () => ({ useCompanySettings: () => ({ snapshot: state.snapshot, sessionGeneration: state.generation, loading: false, saving: false, error: null, conflict: false, lastUpdated: new Date("2026-08-24T12:00:00Z"), save: state.save, retry: state.retry, reloadAfterConflict: state.reload }) }));
afterEach(() => { cleanup(); state.role = "COMPANY_ADMIN"; state.generation = 1; state.snapshot = { etag: "\"7\"", settings: { timezone: "America/Lima", currency: "PEN", geofenceRadiusMeters: 100, trackingIntervalSeconds: 60, locationRetentionDays: 90, saleEditWindowMinutes: 30 } }; state.save.mockReset(); });
test("muestra los valores de política fijos y solo permite guardar el plazo de corrección", () => { render(<CompanySettingsPage />); expect(screen.getByText("100 m")).toBeTruthy(); expect(screen.getByText("Cada 60 s")).toBeTruthy(); expect(screen.getByLabelText("Zona horaria")).toHaveProperty("disabled", true); expect(screen.getByLabelText("Moneda")).toHaveProperty("disabled", true); fireEvent.change(screen.getByLabelText("Plazo para corregir una venta registrada (minutos)"), { target: { value: "45" } }); fireEvent.click(screen.getByRole("button", { name: "Guardar cambios" })); expect(state.save).toHaveBeenCalledWith({ currency: "PEN", saleEditWindowMinutes: 45 }); });
test("presenta la misma configuración como solo lectura al supervisor", () => { state.role = "SUPERVISOR"; render(<CompanySettingsPage />); expect(screen.getByText(/Solo lectura/)).toBeTruthy(); expect(screen.queryByRole("button", { name: "Guardar cambios" })).toBeNull(); expect(screen.getByLabelText("Zona horaria")).toHaveProperty("disabled", true); });
test("al recibir B no muestra ni envía el formulario A", async () => { const view = render(<CompanySettingsPage />); fireEvent.change(screen.getByLabelText("Plazo para corregir una venta registrada (minutos)"), { target: { value: "15" } }); state.snapshot = { etag: "\"8\"", settings: { timezone: "America/Bogota", currency: "COP", geofenceRadiusMeters: 100, trackingIntervalSeconds: 60, locationRetentionDays: 90, saleEditWindowMinutes: 45 } }; view.rerender(<CompanySettingsPage />); await waitFor(() => expect((screen.getByLabelText("Moneda") as HTMLInputElement).value).toBe("COP")); fireEvent.click(screen.getByRole("button", { name: "Guardar cambios" })); expect(state.save).toHaveBeenCalledWith(expect.objectContaining({ currency: "COP", saleEditWindowMinutes: 45 })); expect(state.save).not.toHaveBeenCalledWith(expect.objectContaining({ saleEditWindowMinutes: 15 })); });
test("con ETag igual en B no muestra ni envía los valores A", async () => { const view = render(<CompanySettingsPage />); fireEvent.change(screen.getByLabelText("Plazo para corregir una venta registrada (minutos)"), { target: { value: "15" } }); state.generation = 2; state.snapshot = { etag: "\"7\"", settings: { timezone: "America/Bogota", currency: "COP", geofenceRadiusMeters: 100, trackingIntervalSeconds: 60, locationRetentionDays: 90, saleEditWindowMinutes: 45 } }; view.rerender(<CompanySettingsPage />); await waitFor(() => expect((screen.getByLabelText("Moneda") as HTMLInputElement).value).toBe("COP")); fireEvent.click(screen.getByRole("button", { name: "Guardar cambios" })); expect(state.save).toHaveBeenCalledWith(expect.objectContaining({ currency: "COP", saleEditWindowMinutes: 45 })); expect(state.save).not.toHaveBeenCalledWith(expect.objectContaining({ saleEditWindowMinutes: 15 })); });
